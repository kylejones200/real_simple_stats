[package]
name = "rss-core"
description = "Pure-Rust statistics kernels for real_simple_stats (no Python dependency)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rayon = "1.10"
rand = "0.8"
rand_distr = "0.4"
rand_pcg = "0.3"
faer = "0.19"

[dev-dependencies]
approx = "0.5"
